传统的MVC指的是，用户操作会请求服务端路由，理由会调用对应的控制器来处理，控制器会获取数据，将结果返回给前端，页面重新渲染。
MVVM：传统的前端会将数据手动渲染到页面上，mvvm模式不需要用户收到操作dom元素，将数据绑定到viewmodel层上，会将数据渲染到页面中，视图变化会通知viewModel层更新数据。viewModel就是我们MVVM模式中的桥梁
总结：老的MVC需要通过路由，路由调用controller控制器去请求Model数据库中数据响应返回再操作dom元素来渲染，MVVM，只需要细分了viewmodel层，只需要将数据放在view，view会来给我们处理渲染到页面上